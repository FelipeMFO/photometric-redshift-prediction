-> Ver se coloco a tentativa de vectorização no artigo.
-> Esses magics de memit e time nao tao funcionando pra pegar o tempo de processamento da celula
-> E acho que os increments tb nao sao isso nao

import os
import sys
module_path = os.path.abspath(os.path.join('..'))
if module_path not in sys.path:
    sys.path.append(module_path)
    
    
    
    
IN ORDER TO ANY DEBUG OR ANALYSIS, PICKLES HAVE BEEN CRATED JUST AFTER EACH PREPROCESS STEP
    
    
    
    
    
#module_path = os.path.abspath(os.path.join('..'))
# module_path = os.path.abspath(os.path)
# if module_path not in sys.path:
#     sys.path.append(module_path)

#RAW_PATH = '/data/raw'
#RAW_PATH = os.path.abspath(os.path.join('../..')) + '/redshift_prediction/data/raw'

RAW_PATH = os.path.abspath(os.path.join('../..')) + '/data/raw'
print(RAW_PATH)
